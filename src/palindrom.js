function palindrom(str){
  str = str.toLowerCase(); //крок 1 рядок до одного регістру
  //   str.split(''); // крок 2 розбивалемо рядок на масив
  //   str.reverse(); // крок 3 змінюємо масив місцями 0 індекс стає посліднім, а послідній стає першим і так далі ...
  //   str.join(''); // крок 4 з'єднуємо масив один рядок
  //   так як я написав у горі не можна, тому що ми будемо перевіряти одну тойсамий рядок
  return str ===  str.split('').reverse().join('');
}
console.log(palindrom('123321'));